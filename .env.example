# =========================================================
# Root environment — copy to .env: cp .env.example .env
# Each service also has its own .env; see below for details.
# =========================================================

# OpenAI API key (required by agent_backend)
OPENAI_API_KEY=your_key_here

# --- Service ports (for reference) ---
# FastAPI agent backend  → agent_backend/.env          PORT=8001
# NestJS backend         → backend/multi-agent-app/.env PORT=3001
# Next.js UI             → ui/multi-agent-app/.env.local (runs on 3000 by default)

# --- Inter-service URLs (for reference) ---
# NestJS → FastAPI:  AGENT_BACKEND_URL=http://localhost:8001  (in backend/.env)
# UI → NestJS:       NEXT_PUBLIC_API_URL=http://localhost:3001 (in ui/.env.local)
