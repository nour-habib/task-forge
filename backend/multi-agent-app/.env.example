# Port this NestJS server listens on
PORT=3001

# FastAPI agent backend URL
AGENT_BACKEND_URL=http://localhost:8001

# PostgreSQL connection
# Use 'postgres' user (default superuser) or create your own user
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=postgres
